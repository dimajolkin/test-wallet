language: php
sudo: true
php:
  - 7.1
services:
  - docker
cache:
  directories:
    - $HOME/.composer/cache/files
    - ./bin/.phpunit
    - docker_images

env:
  global:
    - DOCKER_COMPOSE_VERSION=1.24.1
    - SYMFONY_DEPRECATIONS_HELPER=0
    - APP_ENV="test"

before_install:
  - sudo rm /usr/local/bin/docker-compose
  - curl -L https://github.com/docker/compose/releases/download/${DOCKER_COMPOSE_VERSION}/docker-compose-`uname -s`-`uname -m` > docker-compose
  - chmod +x docker-compose
  - sudo mv docker-compose /usr/local/bin

install:
  - docker run -u $UID -v $PWD/var:/app -w /app nouchka/sqlite3 data.sqlite
  - docker-compose build
  - docker-compose up -d
  - sleep 2
  - docker ps
  - docker-compose exec app composer install
  - docker-compose exec app ./bin/console doctrine:schema:create
  - docker-compose exec app ./bin/console doctrine:migrations:migrate -n
  - docker-compose exec app ./bin/console assets:install
  - docker-compose exec app ./vendor/bin/codecept build


script:
  - docker-compose exec app composer check-cs
  - docker-compose exec app composer phpstan
  - docker-compose exec app ./bin/console app:currency-rate-update
  - docker-compose exec app ./vendor/bin/codecept run
